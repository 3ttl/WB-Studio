# v0.9.4 Windows 测试版验证

维护者：Mike Wu。验证日期：2026-09-13。

## eBlot 高分辨率曝光图识别修复

- eBlot 命名与极性规则测试通过：S 前缀曝光图自动建议亮条带分析。
- eBlot 专用高分辨率检测 profile 回归通过；新增模拟 1200 dpi 亮条带测试。
- Python 回归：Bio-Rad 分类 12 项通过；条带识别 15 项通过；源文件编译检查通过。
- Windows v0.9.4 后端健康检查通过，ImageJ 1.54q 引擎可用。
- 数据库未随发布包分发，内置示例仍为程序生成的 P62 / ACTB 合成图。

# v0.9.3 Windows 测试版验证

维护者：Mike Wu。验证日期：2026-09-13。

## v0.9.3 增量扫描与 eBlot 分类

- Bio-Rad 与 eBlot 分类测试 12 项通过，覆盖 M、Merge、S 前缀及 8bit / 16bit 文件名标记。
- 增量导入测试 10 项通过：未变化文件不会再次读取；新增或发生变化的文件才会导入，进度会记录新增、跳过和失败数量。
- 文件库多选和位深筛选测试通过，原有末尾括号分类规则继续有效。
- v0.9.3 Windows 程序正常启动，后台 ImageJ 1.54q 引擎可用；更新前后数据库校验值一致。
- 发布 ZIP 不含用户 data 数据库、运行日志、原始实验图片或项目源码；内置示例为程序生成的 P62 / ACTB 合成图。

以下保留 v0.9.2 及更早版本的验证基线。

# v0.9.2 Windows 测试版验证

维护者：Mike Wu。验证日期：2026-09-11。

## v0.9.2 图库删除与统计导入

- 13 套后端回归检查，共 147 项通过，覆盖导入、保存、恢复链接、ImageJ 实际测量、组合及 PDF 导出；29 套 JavaScript 测试文件通过。
- 删除测试覆盖预览与取消不写文件、状态变化拒绝旧确认、同内容副本保留、回收失败逐项保留链接、扫描后不重新导入、重启后恢复标注、已移除来源不可导出等。
- 统计使用合成比值验证跨图与同膜每组 n=3、4、5、完整导入、不同内参标签明确排除及手动选择保留；单膜与多膜相同数据产生一致结果。
- 隔离 Windows 桌面实际验证图片右键、日期分栏右键、默认仅移除链接、恢复入口、取消和共用图库菜单。测试前后八张合成原图哈希一致。
- Windows 回收站已实际验证一张额外合成图片及其空目录；在不提供回收标记的执行环境中操作被拒绝，无永久删除后备路径。
- 最终压缩包的启动与隐私审查记录在同批 VALIDATION.json；本机用户数据未用于发布包。

以下保留历史版本验证基线。

## v0.9.1 Windows 测试版验证

维护者：Mike Wu。验证日期：2026-09-11。

## v0.9.1 灰度柱状图

本次变化限于结果表格的显示，沿用已有背景校正信号。双条带计算 8 项与工作台 18 项检查通过；不重新计算或改写灰度、比值及复制表格的数据。

在独立 Windows 副本中，使用合成图片和实际 ImageJ 引擎验证柱长与信号成比例、未测量无柱形、过期测量清空柱形、负值方向、零值以及显示反相不影响柱长。图像哈希在操作前后一致。最终发行检查见同批材料的 VALIDATION.json。

以下是 v0.9.0 的完整验证记录，作为此前功能基线保留。本次没有重复完整后端和全部前端测试。

## v0.9.0 自动检查记录

- 本次 240 项 Python 检查通过，覆盖实际 ImageJ 测量、统计、文件读取、保存和 PDF 导出；新增 5 项收藏导出检查。
- 28 套 JavaScript 测试文件全部通过，覆盖工作台、筛选、显示、选框、统计、收藏分组和导出界面。
- 收藏导出检查包括：同图多个选框、按实验与蛋白排序、超过三张换行、无已保存统计组合时导出、显示效果变更后拒绝旧预览、取消收藏后的校验、设置保存与版本冲突。PDF 已实际生成。
- 旧测试素材中遗留的全角示例名称与预期不一致已修正，仍保留 Unicode 规范化、大小写去重和筛选行为的检查。

## v0.9.0 实际 Windows 界面记录

- 独立副本只导入 8 张程序生成的测试图片。在 1440、1280、1080 像素窗口宽度、系统 125% 缩放下，预览保持 16:9，预览区与显示侧栏宽度比为 3:2，无水平溢出；ImageJ 定量参数在下方。
- 滑块与反相按钮正常更新预览，显示调整不改变选框或测量设置；自动聚焦、同次拍摄图片等比显示与手动缩放保留通过检查。
- 收藏总览验证 7 个选框及未画选框的收藏图提示、三列换行、泳道组名实际位置、实验排序、折叠、勾选及共用 PDF 设置。已实际导出两页 PDF。
- 切换页面会关闭图库筛选菜单；重新启动测试副本后，实验顺序和所选条带恢复。
- 操作前后原图 SHA-256 一致；收藏浏览和导出没有改变图像状态，程序正常保存并退出。

## 发行范围

分发材料只含 Windows 程序、运行依赖、文档与合成示例，不含项目源码、个人数据库、运行日志或实验原图。六张 P62 / ACTB 合成示例与上一版逐字节一致，来源见 [示例来源与隐私检查](示例来源与隐私检查.md)。

编译后的程序已经完成上述桌面检查；最终 ZIP 的启动、内容审查与摘要另记在同批发行材料的 VALIDATION.json 和 SHA256SUMS.txt。

这些检查在本机隔离目录完成，未覆盖另一台全新 Windows 电脑，也不代表已经发布到 GitHub 或获得期刊认证。完整示例来源说明见 [示例来源与隐私检查](示例来源与隐私检查.md)。
